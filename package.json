{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic Apollo GraphQL API",
  "scripts": {
    "start": "npx sandbox -p 3000",
    "build": "tsc",
    "test": "echo 'Add tests for your features.'"
  },
  "dependencies": {
    "@architect/sandbox": "latest"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
